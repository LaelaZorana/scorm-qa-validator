# scorm-qa-validator

Quick command-line check for SCORM 1.2 / 2004 packages.

WIP. The thing I want: drop a `.zip` on this and it tells me if the
manifest references files that aren't there, if the schemaversion is one
the LMS will actually accept, if there's a launchable SCO, etc.

Built because I'm tired of uploading to a staging LMS to find out a
manifest is broken.
